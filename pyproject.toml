[tool.poetry]
package-mode = false

[project]
name = "projet-fairness"
version = "0.1.0"
description = "Projet Fairness"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "numpy",
    "scikit-learn",
    "matplotlib",
    "seaborn",
    "jupyter",
    "torch",
    "torch_geometric",
]
